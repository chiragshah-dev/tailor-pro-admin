<div class="row">
  <div class="col-12">
    <div class="card my-4">
      <!-- HEADER -->
      <div class="card-header p-0 position-relative mt-n4 mx-3 z-index-2">
        <div class="bg-gradient-info shadow-info border-radius-lg pt-4 pb-3 d-flex justify-content-between align-items-center">
          <h6 class="text-white text-capitalize ps-3 mb-0">
            System Notifications
          </h6>
          <% unless SystemNotification.exists? %>
            <%= link_to "Add System Notification",
      new_admin_system_notification_path(page: params[:page]),
      class: "btn btn-sm btn-white text-dark text-uppercase font-weight-bold me-3" %>
          <% end %>
        </div>
      </div>
      <!-- BODY -->
      <div class="card-body px-0 pb-2">
        <!-- SEARCH -->
        <div class="px-3 pb-3">
          <%= render "admin/shared/search",
              url: admin_system_notifications_path,
              placeholder: "Search system notifications..." %>
        </div>
        <!-- TABLE -->
        <div class="table-responsive p-0">
          <table class="table align-items-center mb-0">
            <thead>
              <tr>
                <th class="text-uppercase text-dark text-xxs font-weight-bold">Id</th>
                <th class="text-uppercase text-dark text-xxs font-weight-bold ps-2">Title</th>
                <th class="text-uppercase text-dark text-xxs font-weight-bold ps-2">Message</th>
                <th class="text-uppercase text-dark text-xxs font-weight-bold ps-2">Active</th>
                <th class="text-uppercase text-dark text-xxs font-weight-bold text-center">
                  Actions
                </th>
              </tr>
            </thead>
            <tbody>
              <% if @system_notifications.any? %>
                <% @system_notifications.each_with_index do |notification, index| %>
                  <tr>
                    <!-- SERIAL -->
                    <td>
                      <p class="text-xs font-weight-bold mb-0 px-3">
                        <%= (@system_notifications.current_page - 1) * @system_notifications.limit_value + index + 1 %>
                      </p>
                    </td>
                    <!-- KEY -->
                    <td>
                      <p class="text-sm text-secondary mb-0">
                        <%= notification.title %>
                      </p>
                    </td>
                    <!-- TITLE -->
                    <td>
                      <p class="text-sm text-secondary mb-0">
                        <%= truncate(notification.message, length: 60) %>
                      </p>
                    </td>
                    <!-- STATUS -->
                    <td>
                      <span class="badge bg-<%= notification.is_active ? 'success' : 'secondary' %>">
                        <%= notification.is_active ? "yes" : "No" %>
                      </span>
                    </td>
                    <!-- ACTIONS -->
                    <td class="text-center">
                      <div class="d-flex justify-content-center gap-1">
                        <!-- Show -->
                        <%= link_to admin_system_notification_path(notification,page: params[:page]), class: "btn btn-xs rounded btn-secondary", title: "Show" do %>
                          <i class="fas fa-eye"></i>
                        <% end %>
                        <%= link_to edit_admin_system_notification_path(notification,page: params[:page]),
                            class: "btn btn-xs rounded btn-secondary" do %>
                          <i class="fas fa-edit"></i>
                        <% end %>
                        <%= button_to admin_system_notification_path(notification,page: params[:page]),
                            method: :delete,
                            class: "btn btn-xs rounded btn-secondary",
                            data: { turbo_confirm: "Are you sure you want to delete #{notification.title} ?" } do %>
                          <i class="fas fa-trash"></i>
                        <% end %>
                        <%= link_to history_admin_system_notification_path(notification),
      class: "btn btn-xs rounded btn-secondary",
      title: "View History" do %>
                          <i class="fas fa-history"></i>
                        <% end %>
                      </div>
                    </td>
                  </tr>
                <% end %>
              <% else %>
                <tr>
                  <td colspan="12" class="text-center">
                    <p class="no-content-text">No records found.</p>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
        <!-- PAGINATION -->
        <div class="d-flex justify-content-center mt-3">
          <%= paginate @system_notifications %>
        </div>
      </div>
    </div>
  </div>
</div>
